# NETRA AI application configuration

runtime: pytorch

modules:
  iqa: real
  grading: real
  segmentation: real
  xai: real

alerts:
  enabled: false

database:
  path: data/netra.db

sync:
  enabled: true
  interval_seconds: 30
  max_attempts: 5

logging:
  level: INFO
